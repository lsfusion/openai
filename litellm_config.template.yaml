model_list:
  - model_name: __MODEL_ID__
    litellm_params:
      model: __MODEL_ID__

litellm_settings:
  use_litellm_proxy: true
  callbacks: inject_tools.proxy_handler_instance

mcp_servers:
  dmcp:
    url: os.environ/MCP_URL
    transport: "http"